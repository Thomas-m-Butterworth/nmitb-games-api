export interface EndpointTitleBarProps {
  isAuthenticated: boolean;
  apiTitle: string;
}

export interface EndpointTitleProps {
  apiTitle: string;
}

export interface AuthRequirementProps {
  isAuthenticated: boolean;
}
